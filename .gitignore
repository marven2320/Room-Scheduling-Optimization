# Local Claude Code session/tooling config — not part of the app
.claude/

# OS/editor cruft
.DS_Store
*.swp

# Usage-tracking log written by server.py at runtime — contains visitor IP addresses,
# never commit it.
usage_log.csv

# Python bytecode cache
__pycache__/
*.pyc
